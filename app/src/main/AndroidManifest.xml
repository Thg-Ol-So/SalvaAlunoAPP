<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oliveira.salvaaluno">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/img1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VerificaTurma" />
        <activity android:name=".CadastroAtividades" />
        <activity android:name=".Listagem" />
        <activity android:name=".Informacao" />
        <activity
            android:name="Atividades.Atividades"
            android:label="@string/title_activity_atividades"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>